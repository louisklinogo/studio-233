import { router } from '../trpc';

/**
 * Projects router - handles project CRUD operations
 * 
 * This is a placeholder - routers will be moved from src/server/trpc/routers
 * during app migration phase
 */
export const projectsRouter = router({
    // TODO: Move project-related endpoints here
});
