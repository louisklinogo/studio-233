generator client {
  provider        = "prisma-client"
  output          = "../generated"
  moduleFormat    = "esm"
  previewFeatures = ["postgresqlExtensions", "prismaSchemaFolder"]
}

datasource db {
  provider   = "postgresql"
  extensions = [vector]
}
